<?xml version="1.0" encoding="utf-8"?>
<!-- The chrome of item_settings_section.xml's MaterialCardView, as a background drawable, so the
     Xpandable groups on the Player and Reader screens read as the same component as the collapsible
     cards everywhere else. Values match that layout: 22dp radius, colorSurface fill, a 1dp
     colorOutlineVariant stroke, no elevation. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="?attr/colorSurface" />
    <corners android:radius="22dp" />
    <stroke
        android:width="1dp"
        android:color="?attr/colorOutlineVariant" />
</shape>
