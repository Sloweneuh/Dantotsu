<?xml version="1.0" encoding="utf-8"?>
<!--
  Bounded, rounded ripple for the small text/icon buttons that sit in a section header
  ("Show spoilers", the expand chevron). The borderless ripple those used before drew a circle
  sized to the whole header row, which read as a stray blob rather than a press on the button.
-->
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="?attr/colorControlHighlight">
    <item android:id="@android:id/mask">
        <shape android:shape="rectangle">
            <solid android:color="@android:color/white" />
            <corners android:radius="8dp" />
        </shape>
    </item>
</ripple>
