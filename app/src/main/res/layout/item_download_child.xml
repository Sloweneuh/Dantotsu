<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:gravity="center_vertical"
    android:orientation="horizontal"
    android:paddingStart="32dp"
    android:paddingEnd="16dp"
    android:paddingVertical="4dp">

    <androidx.cardview.widget.CardView
        android:id="@+id/itemChildThumbCard"
        android:layout_width="60dp"
        android:layout_height="40dp"
        android:layout_marginEnd="12dp"
        app:cardCornerRadius="6dp"
        app:cardElevation="0dp">

        <ImageView
            android:id="@+id/itemChildThumb"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:contentDescription="@null"
            android:scaleType="centerCrop"
            tools:src="@tools:sample/backgrounds/scenic" />
    </androidx.cardview.widget.CardView>

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical">

        <TextView
            android:id="@+id/itemChildLabel"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:ellipsize="end"
            android:fontFamily="@font/poppins_semi_bold"
            android:maxLines="1"
            android:textSize="13sp"
            tools:text="Chapter 1 - Chapter 10" />

        <TextView
            android:id="@+id/itemChildSize"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:alpha="0.7"
            android:fontFamily="@font/poppins"
            android:maxLines="1"
            android:textSize="11sp"
            tools:text="5.1 MB" />

        <TextView
            android:id="@+id/itemChildDesc"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="2dp"
            android:alpha="0.6"
            android:ellipsize="end"
            android:fontFamily="@font/poppins"
            android:maxLines="2"
            android:textSize="11sp"
            tools:text="Mahiru snaps at Amane for the first time, leaving him unsure of what he did wrong." />
    </LinearLayout>

    <ImageView
        android:id="@+id/itemChildOpenFolder"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:contentDescription="@string/open_folder"
        android:padding="8dp"
        app:srcCompat="@drawable/ic_round_folder_24"
        app:tint="?attr/colorOnBackground" />

    <ImageView
        android:id="@+id/itemChildDelete"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:contentDescription="@string/delete"
        android:padding="8dp"
        app:srcCompat="@drawable/ic_round_delete_24"
        app:tint="?attr/colorOnBackground" />

</LinearLayout>
