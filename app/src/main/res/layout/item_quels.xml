<?xml version="1.0" encoding="utf-8"?><!--
    A row of up to two equally-sized button cards over the media's artwork. Used for the
    prequel/sequel pair and for the watch order / news shortcuts, so the labels, accent colours and
    images are all set in code rather than here.

    A GONE card gives up its weight but not the row's total, so weightSum is what stops a lone card
    stretching the full width. balanceQuelRow() lowers it to widen that case; centre gravity then
    puts the card in the middle of the row rather than leaving it against the start.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/mediaInfoQuelContainer"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="24dp"
    android:layout_marginTop="8dp"
    android:layout_marginBottom="16dp"
    android:gravity="center_horizontal"
    android:orientation="horizontal"
    android:weightSum="2">

    <com.google.android.material.card.MaterialCardView
        android:id="@+id/quelStartCard"
        android:layout_width="0dp"
        android:layout_height="72dp"
        android:layout_margin="8dp"
        android:layout_weight="1"
        android:visibility="gone"
        app:cardCornerRadius="16dp"
        tools:visibility="visible">

        <ImageView
            android:id="@+id/quelStartImage"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:scaleType="centerCrop"
            android:transitionName="mediaCover"
            tools:ignore="ContentDescription"
            tools:src="@tools:sample/backgrounds/scenic" />

        <View
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:alpha="0.75"
            android:background="@color/bg_black" />

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:orientation="vertical">

            <TextView
                android:id="@+id/quelStartLabel"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center"
                android:ellipsize="end"
                android:fontFamily="@font/poppins_bold"
                android:gravity="center"
                android:maxLines="2"
                android:text="@string/prequel"
                android:textAllCaps="true"
                android:textColor="@color/bg_white"
                android:textSize="16sp" />

            <View
                android:id="@+id/quelStartAccent"
                android:layout_width="64dp"
                android:layout_height="2dp"
                android:layout_gravity="center"
                android:background="?attr/colorSecondary" />
        </LinearLayout>
    </com.google.android.material.card.MaterialCardView>

    <com.google.android.material.card.MaterialCardView
        android:id="@+id/quelEndCard"
        android:layout_width="0dp"
        android:layout_height="72dp"
        android:layout_margin="8dp"
        android:layout_weight="1"
        android:visibility="gone"
        app:cardCornerRadius="16dp"
        tools:visibility="visible">

        <ImageView
            android:id="@+id/quelEndImage"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:scaleType="centerCrop"
            android:transitionName="mediaCover"
            tools:ignore="ContentDescription"
            tools:src="@tools:sample/backgrounds/scenic" />

        <View
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:alpha="0.75"
            android:background="@color/bg_black" />

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:orientation="vertical">

            <TextView
                android:id="@+id/quelEndLabel"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center"
                android:ellipsize="end"
                android:fontFamily="@font/poppins_bold"
                android:gravity="center"
                android:maxLines="2"
                android:text="@string/sequel"
                android:textAllCaps="true"
                android:textColor="@color/bg_white"
                android:textSize="16sp" />

            <View
                android:id="@+id/quelEndAccent"
                android:layout_width="64dp"
                android:layout_height="2dp"
                android:layout_gravity="center"
                android:background="?attr/colorPrimary" />
        </LinearLayout>

    </com.google.android.material.card.MaterialCardView>

</LinearLayout>
