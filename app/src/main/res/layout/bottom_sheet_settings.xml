<?xml version="1.0" encoding="utf-8"?>
<androidx.core.widget.NestedScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bottom_sheet_background">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

            <LinearLayout
                android:id="@+id/settingsAccountRow"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_gravity="bottom"
                android:orientation="horizontal"
                android:padding="16dp">

                <com.google.android.material.card.MaterialCardView
                    android:id="@+id/homeUserAvatarContainer"
                    android:layout_width="52dp"
                    android:layout_height="52dp"
                    android:layout_margin="4dp"
                    android:backgroundTint="@color/nav_bg_inv"
                    app:cardCornerRadius="26dp">

                    <com.google.android.material.imageview.ShapeableImageView
                        android:id="@+id/settingsUserAvatar"
                        android:layout_width="52dp"
                        android:layout_height="52dp"
                        android:scaleType="center"
                        app:srcCompat="@drawable/ic_round_person_24"
                        tools:ignore="ContentDescription,ImageContrastCheck" />

                </com.google.android.material.card.MaterialCardView>

                <LinearLayout
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_marginStart="16dp"
                    android:layout_weight="1"
                    android:gravity="center_vertical"
                    android:minHeight="64dp"
                    android:orientation="vertical">

                    <TextView
                        android:id="@+id/settingsUsername"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:layout_marginBottom="-4dp"
                        android:fontFamily="@font/poppins_bold"
                        android:gravity="center"
                        android:text="@string/username"
                        android:textSize="16sp" />

                    <TextView
                        android:id="@+id/settingsLogin"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:layout_marginStart="-16dp"
                        android:layout_marginTop="-16dp"
                        android:layout_marginBottom="-16dp"
                        android:fontFamily="@font/poppins_bold"
                        android:padding="16dp"
                        android:text="@string/logout"
                        android:textColor="?attr/colorSecondary"
                        android:textSize="14sp" />

                </LinearLayout>


                <!-- Cloud sync at a glance. Sync is silent by design, so without this there is
                     nothing anywhere that distinguishes "off", "working" and "needs you". -->
                <com.google.android.material.card.MaterialCardView
                    android:id="@+id/settingsCloudCard"
                    android:layout_width="52dp"
                    android:layout_height="52dp"
                    android:layout_marginTop="4dp"
                    android:layout_marginEnd="12dp"
                    android:backgroundTint="@color/nav_bg_inv"
                    app:cardCornerRadius="26dp"
                    app:strokeColor="@color/transparent">

                    <com.google.android.material.imageview.ShapeableImageView
                        android:id="@+id/settingsCloudStatus"
                        android:layout_width="52dp"
                        android:layout_height="52dp"
                        android:contentDescription="@string/cloud_sync"
                        android:scaleType="center"
                        app:srcCompat="@drawable/ic_round_cloud_off_24"
                        app:tint="@color/bg_opp"
                        tools:ignore="ImageContrastCheck" />

                </com.google.android.material.card.MaterialCardView>

                <FrameLayout
                    android:id="@+id/settingsNotificationContainer"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="4dp"
                    android:layout_marginEnd="16dp">

                    <com.google.android.material.card.MaterialCardView
                        android:layout_width="52dp"
                        android:layout_height="52dp"
                        app:strokeColor="@color/transparent"
                        android:backgroundTint="@color/nav_bg_inv"
                        app:cardCornerRadius="26dp">

                        <com.google.android.material.imageview.ShapeableImageView
                            android:id="@+id/settingsNotification"
                            android:layout_width="52dp"
                            android:layout_height="52dp"
                            android:scaleType="center"
                            app:srcCompat="@drawable/ic_round_notifications_active_24"
                            app:tint="@color/bg_opp"
                            tools:ignore="ContentDescription,ImageContrastCheck" />

                    </com.google.android.material.card.MaterialCardView>

                    <TextView
                        android:id="@+id/settingsNotificationCount"
                        android:layout_width="18dp"
                        android:layout_height="18dp"
                        android:layout_gravity="end|bottom"
                        android:background="@drawable/notification_circle"
                        android:fontFamily="@font/poppins_semi_bold"
                        android:gravity="center"
                        android:textColor="#F3F3F3"
                        android:textSize="12sp"
                        android:visibility="gone"
                        tools:ignore="SmallSp"
                        tools:text="1"
                        tools:visibility="visible" />
                </FrameLayout>
            </LinearLayout>

            <!-- Quick tiles. The row above is the account and stays fixed; everything below
                 it is whatever the user arranged. -->
            <FrameLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:paddingStart="12dp"
                android:paddingEnd="12dp">

                <androidx.viewpager2.widget.ViewPager2
                    android:id="@+id/quickTilesPager"
                    android:layout_width="match_parent"
                    android:layout_height="320dp" />

                <!-- Editing is one continuous surface instead of pages: the arrangement has to be
                     reorderable as a whole, with the catalogue shelf below it. -->
                <LinearLayout
                    android:id="@+id/quickTilesEditContainer"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:orientation="vertical"
                    android:visibility="gone"
                    tools:visibility="gone">

                    <FrameLayout
                        android:layout_width="match_parent"
                        android:layout_height="48dp">

                        <ImageButton
                            android:id="@+id/quickTilesEditDone"
                            android:layout_width="40dp"
                            android:layout_height="40dp"
                            android:layout_gravity="start|center_vertical"
                            android:background="?attr/selectableItemBackgroundBorderless"
                            android:contentDescription="@string/quick_tiles_done"
                            android:padding="8dp"
                            android:src="@drawable/ic_round_arrow_back_ios_new_24"
                            app:tint="?attr/colorOnBackground" />

                        <LinearLayout
                            android:layout_width="wrap_content"
                            android:layout_height="wrap_content"
                            android:layout_gravity="end|center_vertical"
                            android:gravity="center_vertical"
                            android:orientation="horizontal">

                            <!-- Appears only once there is something to undo, as in the system
                                 panel. The arrangement saves as you go, so this is the way back
                                 from a mistaken removal. -->
                            <ImageButton
                                android:id="@+id/quickTilesUndo"
                                android:layout_width="36dp"
                                android:layout_height="36dp"
                                android:layout_marginEnd="4dp"
                                android:background="?attr/selectableItemBackgroundBorderless"
                                android:contentDescription="@string/quick_tiles_undo"
                                android:padding="7dp"
                                android:src="@drawable/ic_round_undo_24"
                                android:visibility="gone"
                                app:tint="?attr/colorOnBackground"
                                tools:visibility="visible" />

                            <com.google.android.material.button.MaterialButton
                                android:id="@+id/quickTilesRemove"
                                style="@style/Widget.Material3.Button"
                                android:layout_width="wrap_content"
                                android:layout_height="40dp"
                                android:fontFamily="@font/poppins_bold"
                                android:insetTop="0dp"
                                android:insetBottom="0dp"
                                android:minWidth="0dp"
                                android:paddingStart="20dp"
                                android:paddingEnd="20dp"
                                android:text="@string/quick_tiles_remove"
                                android:textAllCaps="false"
                                android:textSize="13sp"
                                app:cornerRadius="20dp" />

                        </LinearLayout>

                    </FrameLayout>

                    <androidx.recyclerview.widget.RecyclerView
                        android:id="@+id/quickTilesEditor"
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:clipChildren="false"
                        android:nestedScrollingEnabled="false"
                        android:overScrollMode="never"
                        tools:itemCount="6"
                        tools:listitem="@layout/item_quick_tile" />

                    <!-- Below the whole catalogue rather than up in the toolbar, where the system
                         panel puts it: throwing the arrangement away is the last thing on the
                         screen, not something to brush past on the way in. -->
                    <com.google.android.material.button.MaterialButton
                        android:id="@+id/quickTilesReset"
                        style="@style/Widget.Material3.Button"
                        android:layout_width="wrap_content"
                        android:layout_height="40dp"
                        android:layout_gravity="center_horizontal"
                        android:layout_marginTop="16dp"
                        android:layout_marginBottom="8dp"
                        android:fontFamily="@font/poppins_bold"
                        android:insetTop="0dp"
                        android:insetBottom="0dp"
                        android:minWidth="0dp"
                        android:paddingStart="24dp"
                        android:paddingEnd="24dp"
                        android:text="@string/quick_tiles_reset"
                        android:textAllCaps="false"
                        android:textSize="13sp"
                        app:cornerRadius="20dp" />

                </LinearLayout>

            </FrameLayout>

            <FrameLayout
                android:layout_width="match_parent"
                android:layout_height="48dp"
                android:paddingStart="16dp"
                android:paddingEnd="8dp">

                <!-- Where the system panel puts its build number: quiet, on the same line as the
                     page dots, out of the way of everything that is actually a control. -->
                <TextView
                    android:id="@+id/quickTilesVersion"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_gravity="start|center_vertical"
                    android:alpha="0.5"
                    android:ellipsize="end"
                    android:fontFamily="@font/poppins_family"
                    android:maxLines="1"
                    android:textSize="11sp"
                    tools:text="Dantotsu v4.9.1" />

                <ani.dantotsu.settings.quicktiles.QuickTilePageIndicator
                    android:id="@+id/quickTilesIndicator"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_gravity="center" />

                <ImageButton
                    android:id="@+id/quickTilesEdit"
                    android:layout_width="40dp"
                    android:layout_height="40dp"
                    android:layout_gravity="end|center_vertical"
                    android:background="?attr/selectableItemBackgroundBorderless"
                    android:contentDescription="@string/quick_tiles_edit"
                    android:padding="8dp"
                    android:src="@drawable/ic_round_edit_24"
                    app:tint="?attr/colorOnBackground" />

            </FrameLayout>

    </LinearLayout>

</androidx.core.widget.NestedScrollView>
