<?xml version="1.0" encoding="utf-8"?><!--
    Portrait clip composer. The preview and trim controls are pinned above a scrolling column of
    options, so the thing being edited never leaves the screen while the options are adjusted.
    See layout-land for the side-by-side arrangement the player's own orientation gets.
-->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bottom_sheet_background">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <androidx.cardview.widget.CardView
            android:layout_width="32dp"
            android:layout_height="4dp"
            android:layout_gravity="center_horizontal"
            android:layout_marginTop="8dp"
            app:cardBackgroundColor="?attr/colorPrimary"
            app:cardCornerRadius="2dp"
            app:cardElevation="0dp" />

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:fontFamily="@font/poppins"
            android:gravity="center_horizontal"
            android:text="@string/clip"
            android:textSize="20sp"
            android:textStyle="bold" />

        <!-- One scroll for the whole composer, card and options together.
             Splitting them into two scrolling regions meant each was capped at its share of the
             sheet, and on a short screen the options' share was too small to use. A single scroll
             has no such ceiling: everything is always reachable by scrolling, and with the options
             reduced to chips the card is rarely pushed far off the top. -->
        <androidx.core.widget.NestedScrollView
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical">

                <include
                    android:id="@+id/clipStage"
                    layout="@layout/view_clip_stage"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content" />

                <include
                    android:id="@+id/clipOptions"
                    layout="@layout/view_clip_options"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content" />

                <!-- Leaves room for the floating action row below. -->
                <Space
                    android:layout_width="match_parent"
                    android:layout_height="80dp" />

            </LinearLayout>

        </androidx.core.widget.NestedScrollView>

    </LinearLayout>

    <include
        android:id="@+id/clipBar"
        layout="@layout/view_clip_actions"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom" />

    <!-- Export progress. Covers the sheet so the card can't be edited mid-render. -->
    <LinearLayout
        android:id="@+id/clipProgressOverlay"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="#CC000000"
        android:clickable="true"
        android:focusable="true"
        android:gravity="center"
        android:orientation="vertical"
        android:visibility="gone"
        tools:visibility="gone">

        <com.google.android.material.progressindicator.CircularProgressIndicator
            android:id="@+id/clipProgressBar"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:indeterminate="false"
            app:indicatorSize="64dp"
            app:trackThickness="6dp" />

        <TextView
            android:id="@+id/clipProgressLabel"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:fontFamily="@font/poppins_bold"
            android:textColor="@android:color/white"
            android:textSize="15sp"
            tools:text="Rendering clip… 42%" />

        <Button
            android:id="@+id/clipProgressCancel"
            style="@style/Widget.Material3.Button.OutlinedButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="24dp"
            android:fontFamily="@font/poppins_bold"
            android:text="@string/cancel"
            app:cornerRadius="16dp"
            app:strokeColor="?attr/colorPrimaryContainer" />

    </LinearLayout>
</FrameLayout>
